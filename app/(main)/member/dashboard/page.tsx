import { redis } from "@/lib/redis";
import React from "react";

const page = async () => {
  // const cached = await redis.get(`user: jj`);
  // console.log(cached);

  return <div>dashboradpage</div>;
};

export default page;
